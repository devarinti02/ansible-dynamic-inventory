---
plugin: amazon.aws.aws_ec2
regions:
  -ap-southeast-1

keyed_groups:
  - key: tag.Name
    prefix: abcd

filter:
  instance-state-name: started

hostnames:


compose:
-ansible_host:
-ansible_user:
-ansible_ssh_private_key_files: /home/ubuntu/ap-southeast-1